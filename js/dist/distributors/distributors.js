document.addEventListener('DOMContentLoaded', () => { const termsScrollArea = document.getElementById('terms-scroll-area'); const agreeCheckbox = document.getElementById('agree-checkbox'); const registerButton = document.getElementById('register-button'); const revisionDate = document.querySelector('.revision-date'); if (!termsScrollArea || !revisionDate || !agreeCheckbox || !registerButton) { console.error('必要な要素がHTMLに見つかりません。'); return; } const scrollTarget = termsScrollArea; let isScrolledToBottom = false; const checkScroll = () => { const scrollDifference = termsScrollArea.scrollHeight - termsScrollArea.clientHeight; const tolerance = 5; if (termsScrollArea.scrollTop >= scrollDifference - tolerance) { if (!isScrolledToBottom) { agreeCheckbox.disabled = false; isScrolledToBottom = true; scrollTarget.removeEventListener('scroll', checkScroll); console.log('利用規約の最後までスクロールされました。チェックボックスが有効になりました。'); } } }; scrollTarget.addEventListener('scroll', checkScroll); checkScroll(); agreeCheckbox.addEventListener('change', () => { if (agreeCheckbox.checked) { registerButton.classList.remove('disabled-button'); registerButton.href = './regist_form.html'; } else { registerButton.classList.add('disabled-button'); registerButton.href = '#'; } }); });