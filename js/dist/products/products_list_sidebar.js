document.addEventListener('DOMContentLoaded', () => { const mainTable = document.querySelector('body > table[width="1000"]'); const mainContentElement = document.querySelector('main'); const footerPlaceholder = document.getElementById('footer-placeholder'); const waitForHeader = (callback) => { const checkElement = () => { const headerElement = document.getElementById('header'); if (headerElement) { if (document.readyState === 'complete') { callback(headerElement); } else { window.addEventListener('load', () => callback(headerElement)); } console.log('detect header'); } else { if (Date.now() - startTime > 5000) { console.warn("Header element not found after 5 seconds. Assuming top offset is 10px."); if (document.readyState === 'complete') { callback(null); } else { window.addEventListener('load', () => callback(null)); } } else { setTimeout(checkElement, 50); } } }; const startTime = Date.now(); checkElement(); }; waitForHeader((headerElement) => { console.log('start wfh'); if (!(mainTable || mainContentElement) || !footerPlaceholder) { console.warn("主要な要素（テーブルまたはフッター）が見つかりません。"); return; } const sidebarContainer = document.createElement('div'); sidebarContainer.className = 'product-sidebar-container'; sidebarContainer.classList.add('sidebar-closed'); const contentWrapper = document.createElement('div'); contentWrapper.className = 'product-detail-page-wrapper'; if (mainTable) { mainTable.parentNode.removeChild(mainTable); console.log('maintable'); contentWrapper.appendChild(mainTable); } else if (mainContentElement) { mainContentElement.parentNode.removeChild(mainContentElement); console.log('main'); contentWrapper.appendChild(mainContentElement); } const overlay = document.createElement('div'); overlay.id = 'sidebar-overlay'; overlay.style.opacity = '0'; overlay.style.display = 'none'; footerPlaceholder.parentNode.insertBefore(sidebarContainer, footerPlaceholder); footerPlaceholder.parentNode.insertBefore(contentWrapper, footerPlaceholder); footerPlaceholder.parentNode.insertBefore(overlay, contentWrapper); const controlWrapper = document.createElement('div'); controlWrapper.id = 'sidebar-control-wrapper'; controlWrapper.style.left = '10px'; const toggleButton = document.createElement('button'); toggleButton.id = 'sidebar-toggle-button'; toggleButton.className = 'sidebar-toggle-btn'; toggleButton.innerHTML = '<span></span><span></span><span></span>'; toggleButton.setAttribute('aria-label', '製品カテゴリーメニューを開く'); controlWrapper.appendChild(toggleButton); document.body.appendChild(controlWrapper); const closeButton = document.createElement('button'); closeButton.id = 'sidebar-close-button'; closeButton.className = 'sidebar-close-btn'; closeButton.innerHTML = '&times;'; closeButton.setAttribute('aria-label', '製品カテゴリーメニューを閉じる'); closeButton.style.display = 'none'; sidebarContainer.appendChild(closeButton); const setSidebarPositionsAndHeight = () => { const isSidebarOpen = sidebarContainer.classList.contains('sidebar-open'); let topOffset; if (isSidebarOpen) { topOffset = 0; } else if (headerElement) { const isHeaderHidden = headerElement.classList.contains('is-hidden'); if (isHeaderHidden) { topOffset = 10; } else { const headerHeight = headerElement.offsetHeight; topOffset = headerHeight + 10; } } else { topOffset = 10; } sidebarContainer.style.top = `${topOffset}px`; controlWrapper.style.setProperty('top', `${topOffset}px`, 'important'); const sidebarTop = parseInt(sidebarContainer.style.top, 10) || 0; const calculatedHeight = window.innerHeight - sidebarTop; sidebarContainer.style.maxHeight = `${calculatedHeight}px`; }; const toggleSidebar = () => { const isClosed = sidebarContainer.classList.contains('sidebar-closed'); sidebarContainer.classList.toggle('sidebar-closed', !isClosed); sidebarContainer.classList.toggle('sidebar-open', isClosed); if (controlWrapper) { controlWrapper.style.display = isClosed ? 'none' : 'block'; } closeButton.style.display = isClosed ? 'block' : 'none'; if (isClosed) { overlay.style.display = 'block'; setTimeout(() => { overlay.style.opacity = '1'; }, 10); setSidebarPositionsAndHeight(); } else { overlay.style.opacity = '0'; setTimeout(() => { overlay.style.display = 'none'; setSidebarPositionsAndHeight(); }, 300); } }; toggleButton.addEventListener('click', toggleSidebar); closeButton.addEventListener('click', toggleSidebar); overlay.addEventListener('click', toggleSidebar); if (headerElement) { const observer = new MutationObserver(mutations => { mutations.forEach(mutation => { if (mutation.type === 'attributes' && mutation.attributeName === 'class') { setSidebarPositionsAndHeight(); } }); }); observer.observe(headerElement, { attributes: true }); } const handleResize = () => { setSidebarPositionsAndHeight(); }; window.addEventListener('resize', handleResize); setSidebarPositionsAndHeight(); if (controlWrapper) { controlWrapper.style.opacity = '1'; } fetch('/html/products/products_data.json')
.then(response => { if (!response.ok) { throw new Error('Network response was not ok'); } return response.json(); })
.then(data => { const currentUrlPath = window.location.pathname.replace(/\/+$/, '') + window.location.search; const title = document.createElement('h3'); title.textContent = '製品一覧'; sidebarContainer.appendChild(title); const caution1 = document.createElement('p'); caution1.textContent = '※青文字は詳細ページにリンクします。'; caution1.style.fontSize = '12px'
caution1.style.color = '#00479d'
sidebarContainer.appendChild(caution1); const productList = document.createElement('ul'); productList.className = 'category-list'; sidebarContainer.appendChild(productList); data.products_data.categories.forEach(category => { const categoryItem = document.createElement('li'); categoryItem.className = 'category-item'; const categoryHeader = document.createElement('div'); categoryHeader.className = 'category-header'; const categoryName = document.createElement('strong'); categoryName.textContent = category.name; const categoryToggleBtn = document.createElement('img'); categoryToggleBtn.src = '/images/common/right_arrow_icon.png'; categoryToggleBtn.alt = 'トグル'; categoryToggleBtn.className = 'toggle-btn'; categoryHeader.appendChild(categoryName); categoryHeader.appendChild(categoryToggleBtn); categoryItem.appendChild(categoryHeader); const productsUl = document.createElement('ul'); productsUl.className = 'products-sublist'; productsUl.style.display = 'none'; categoryItem.appendChild(productsUl); let isCurrentCategory = false; category.products.forEach(product => { const productListItem = document.createElement('li'); const productDiv = document.createElement('div'); productDiv.className = 'product-item-header'; const productLink = document.createElement('a'); productLink.href = product.main_page.url; productLink.textContent = product.name; productDiv.appendChild(productLink); let isCurrentProduct = false; const productUrlPath = product.main_page.url.replace(/\/+$/, ''); if (currentUrlPath.endsWith(productUrlPath)) { productDiv.classList.add('current-product'); isCurrentCategory = true; isCurrentProduct = true; } if (product.sub_pages && product.sub_pages.length > 0) { const hasValidSubPages = product.sub_pages.some(subPage => subPage.url); if (hasValidSubPages) { const productToggleBtn = document.createElement('img'); productToggleBtn.src = '/images/common/right_arrow_icon.png'; productToggleBtn.alt = 'トグル'; productToggleBtn.className = 'toggle-btn sub-toggle-btn'; productDiv.appendChild(productToggleBtn); const subPagesUl = document.createElement('ul'); subPagesUl.className = 'sub-pages-list'; subPagesUl.style.display = 'none'; product.sub_pages.forEach(subPage => { const subPageListItem = document.createElement('li'); const subPageLink = document.createElement('a'); let isInsert = false; if (subPage.url) { subPageLink.href = subPage.url; subPageLink.style.color = "#00479d"; const subPageUrlPath = subPage.url.replace(/\/+$/, ''); if (currentUrlPath.endsWith(subPageUrlPath)) { subPageLink.classList.add('current-product'); productDiv.classList.add('current-product'); isCurrentCategory = true; isCurrentProduct = true; isInsert = true; } } else { subPageLink.style.pointerEvents = 'none'; } subPageLink.textContent = subPage.name; subPageListItem.appendChild(subPageLink); subPagesUl.appendChild(subPageListItem); }); productListItem.appendChild(productDiv); productListItem.appendChild(subPagesUl); productsUl.appendChild(productListItem); productDiv.addEventListener('click', () => { if (subPagesUl.style.display === 'none') { subPagesUl.style.display = 'block'; productToggleBtn.style.transform = 'rotate(90deg)'; } else { subPagesUl.style.display = 'none'; productToggleBtn.style.transform = 'rotate(0deg)'; } }); } else { productListItem.appendChild(productDiv); productsUl.appendChild(productListItem); } } else { productListItem.appendChild(productDiv); productsUl.appendChild(productListItem); } }); productList.appendChild(categoryItem); categoryHeader.addEventListener('click', () => { if (productsUl.style.display === 'none') { productsUl.style.display = 'block'; categoryToggleBtn.style.transform = 'rotate(90deg)'; } else { productsUl.style.display = 'none'; categoryToggleBtn.style.transform = 'rotate(0deg)'; } }); if (isCurrentCategory) { productsUl.style.display = 'block'; categoryToggleBtn.style.transform = 'rotate(90deg)'; } }); })
.catch(error => { console.error('製品データの読み込みに失敗しました:', error); sidebarContainer.style.display = 'none'; }); }); });