const manufacturerKeys = ["toyota", "lexus", "nissan", "honda", "mitsubishi", "subaru", "suzuki", "daihatsu", "mazda", "isuzu"]; const pdfListContainer = document.getElementById('pdf-list-container'); const loadingMessage = document.getElementById('loading-message'); async function getPdfLastModified(url) { try { const response = await fetch(url, { method: 'HEAD' }); if (response.ok) { const lastModified = response.headers.get('Last-Modified'); if (lastModified) { const date = new Date(lastModified); const year = date.getFullYear(); const month = String(date.getMonth() + 1).padStart(2, '0'); const day = String(date.getDate()).padStart(2, '0'); return `${year}.${month}.${day}`; } } return '日付取得失敗'; } catch (error) { console.error('Error fetching file date:', error); return '日付取得失敗'; } } function groupDataByCategory(data) { const groupedData = new Map(); let currentCategory = null; for (const item of data) { if (item.type === "category") { currentCategory = item.categoryName; if (!groupedData.has(currentCategory)) { groupedData.set(currentCategory, []); } } else if (currentCategory) { groupedData.get(currentCategory).push(item); } } return groupedData; } async function renderCategoryTables(data) { if (!pdfListContainer) return; if (loadingMessage) { loadingMessage.remove(); } const groupedData = groupDataByCategory(data); pdfListContainer.innerHTML = ''; for (const [categoryName, products] of groupedData.entries()) { if (products.length === 0) continue; const details = document.createElement('details'); details.classList.add('category-details'); const summary = document.createElement('summary'); summary.classList.add('category-summary'); summary.innerHTML = categoryName; details.appendChild(summary); const tableWrapper = document.createElement('div'); tableWrapper.classList.add('table-wrapper'); const topScroller = document.createElement('div'); topScroller.id = `top-scroller-${categoryName.replace(/[^a-zA-Z0-9]/g, '')}`; topScroller.classList.add('top-scroller'); topScroller.innerHTML = '<div></div>'; details.addEventListener('toggle', () => { if (details.open) { setTimeout(() => { syncTableWidths(details); setupScrollSynchronization(details); }, 0); } }); details.appendChild(topScroller); const table = document.createElement('table'); const thead = document.createElement('thead'); thead.innerHTML = `
<tr>
<th>製品名</th>
<th>区分</th>
<th>補足</th>
<th>トヨタ</th>
<th>レクサス</th>
<th>ニッサン</th>
<th>ホンダ</th>
<th>ミツビシ</th>
<th>スバル</th>
<th>スズキ</th>
<th>ダイハツ</th>
<th>マツダ</th>
<th>イスズ</th>
</tr>
`; table.appendChild(thead); const tbody = document.createElement('tbody'); for (const product of products) { product.rows.forEach((rowData, rowIndex) => { const tr = document.createElement('tr'); const tdProduct = document.createElement('td'); tdProduct.classList.add('first-column'); if (rowIndex === 0) { const a = document.createElement('a'); a.href = product.productLink; a.textContent = product.productName; tdProduct.appendChild(a); tdProduct.classList.add('product-name-cell'); } else { tdProduct.textContent = '同上'; tdProduct.classList.add('ditto-cell'); } tr.appendChild(tdProduct); const tdCategory = document.createElement('td'); tdCategory.textContent = rowData.category; tr.appendChild(tdCategory); const tdCategory2 = document.createElement('td'); tdCategory2.innerHTML = rowData.category2; tr.appendChild(tdCategory2); if (rowData.links.note) { const tdNote = document.createElement('td'); tdNote.setAttribute('colspan', 10); tdNote.classList.add('bg-yellow'); const a = document.createElement('a'); a.href = rowData.links.link; a.textContent = rowData.links.note; tdNote.appendChild(a); tr.appendChild(tdNote); } else { for (const manufacturer of manufacturerKeys) { const linkData = rowData.links[manufacturer]; const tdLink = document.createElement('td'); if (!linkData || linkData.pdf === "-") { tdLink.textContent = "-"; } else if (linkData.pdf === "動作未確認") { tdLink.textContent = "動作未確認"; } else { const a = document.createElement('a'); a.href = linkData.pdf; a.target = "_blank"; const img = document.createElement('img'); img.src = "../../images/common/pdf_icon.webp"; img.alt = "PDFアイコン"; img.classList.add('pdf-icon'); a.appendChild(img); const dateElement = document.createElement('div'); dateElement.textContent = '日付取得中...'; if (window.innerWidth < 600) { dateElement.style.fontSize = "0.65em"; } else { dateElement.style.fontSize = "0.85em"; } dateElement.style.marginTop = "5px"; dateElement.classList.add('date-text'); tdLink.appendChild(a); tdLink.appendChild(dateElement); getPdfLastModified(linkData.pdf).then(date => { dateElement.textContent = `${date} 更新`; }).catch(() => { dateElement.textContent = '取得エラー'; }); } tr.appendChild(tdLink); } } tbody.appendChild(tr); }); } table.appendChild(tbody); tableWrapper.appendChild(table); details.appendChild(tableWrapper); pdfListContainer.appendChild(details); } } function syncTableWidths(detailsElement) { const tableWrapper = detailsElement.querySelector('.table-wrapper'); const table = detailsElement.querySelector('table'); const headerCells = table ? table.querySelectorAll('thead th') : []; const bodyRows = table ? table.querySelectorAll('tbody tr') : []; const topScrollerInner = detailsElement.querySelector('.top-scroller > div'); if (!table || bodyRows.length === 0 || !topScrollerInner) { return; } let firstDataRowCells = null; for (const row of bodyRows) { const cells = row.querySelectorAll('td'); if (cells.length === headerCells.length) { firstDataRowCells = cells; break; } } if (firstDataRowCells) { headerCells.forEach((headerCell, index) => { if (firstDataRowCells[index]) { const cellWidth = firstDataRowCells[index].offsetWidth; headerCell.style.width = `${cellWidth}px`; headerCell.style.minWidth = `${cellWidth}px`; headerCell.style.boxSizing = 'border-box'; } }); } const scrollWidth = table.scrollWidth; topScrollerInner.style.width = `${scrollWidth}px`; } function setupScrollSynchronization(detailsElement) { const topScroller = detailsElement.querySelector('.top-scroller'); const tableWrapper = detailsElement.querySelector('.table-wrapper'); if (!topScroller || !tableWrapper) return; let isSyncing = false; topScroller.addEventListener('scroll', () => { if (!isSyncing) { isSyncing = true; tableWrapper.scrollLeft = topScroller.scrollLeft; setTimeout(() => { isSyncing = false; }, 0); } }, { passive: true }); tableWrapper.addEventListener('scroll', () => { if (!isSyncing) { isSyncing = true; topScroller.scrollLeft = tableWrapper.scrollLeft; setTimeout(() => { isSyncing = false; }, 0); } }, { passive: true }); } document.addEventListener('DOMContentLoaded', () => { fetch('./match_pdf.json')
.then(response => { if (!response.ok) { throw new Error(`match_pdf.json の読み込みに失敗しました (${response.status})`); } return response.json(); })
.then(data => { renderCategoryTables(data); })
.catch(error => { console.error('There was a problem with the fetch operation:', error); if (pdfListContainer) { pdfListContainer.innerHTML = '<p style="color: red; text-align: left;">適合データの読み込みに失敗しました。ファイルパスまたはJSON形式を確認してください。</p>'; } }); window.addEventListener('resize', () => { document.querySelectorAll('.category-details[open]').forEach(details => { syncTableWidths(details); }); }); });