import { PRODUCTS_DATA } from './data_mapper.js'; import { getSavedItemsMap, setSavedItemsMap, getRowUniqueId, createPriceCellHtml, createNotesCellHtml } from './result_renderer.js'; const savedItemsDialog = document.getElementById('saved-items-dialog'); const showDialogButton = document.getElementById('show-saved-items-button'); const closeDialogButton = document.getElementById('close-saved-items-button'); const savedItemsListContainer = document.getElementById('saved-items-list'); function renderSavedItemsList() { if (!savedItemsListContainer) return; const savedData = getSavedItemsMap(); const productNames = Object.keys(savedData); savedItemsListContainer.innerHTML = ''; if (productNames.length === 0) { savedItemsListContainer.innerHTML = '<p>保存されている適合リストはありません。</p>'; return; } productNames.forEach(productName => { const productData = savedData[productName]; const headerData = productData.headerData; const items = productData.items; const itemValues = Object.values(items); const allColumns = headerData.flatMap(h => h.subHeaders); const productSection = document.createElement('div'); productSection.className = 'product-section'; const title = document.createElement('h4'); title.textContent = `製品: ${getProductName(productName)}`; productSection.appendChild(title); const table = document.createElement('table'); table.className = 'saved-items-table'; const thead = document.createElement('thead'); const headerRow = document.createElement('tr'); headerData.forEach(header => { const th = document.createElement('th'); th.textContent = header.label; th.setAttribute('colspan', header.subHeaders.length); th.className = 'saved-th saved-th-group'; headerRow.appendChild(th); }); thead.appendChild(headerRow); const subHeaderRow = document.createElement('tr'); allColumns.forEach(col => { const thSub = document.createElement('th'); if (col.label === 'nav_col_2') { thSub.textContent = 'サービスホールスイッチ切替タイプ/LEDスイッチ切替タイプ_2'; } else { thSub.textContent = col.label; } thSub.className = 'saved-th saved-th-sub'; subHeaderRow.appendChild(thSub); }); const actionTh = document.createElement('th'); actionTh.textContent = '操作'; actionTh.className = 'saved-th saved-th-action'; subHeaderRow.appendChild(actionTh); thead.appendChild(subHeaderRow); table.appendChild(thead); const tbody = document.createElement('tbody'); itemValues.forEach(item => { const rowId = getRowUniqueId(item); const row = document.createElement('tr'); row.className = 'saved-tr'; allColumns.forEach(col => { const td = document.createElement('td'); td.className = 'saved-td'; if (col.priceKeys) { td.innerHTML = createPriceCellHtml(item, col); } else if (col.key === 'notes') { td.innerHTML = createNotesCellHtml(item); } else { td.innerHTML = (item[col.key] || '').replace(/\n/g, '<br>'); } row.appendChild(td); }); const actionTd = document.createElement('td'); actionTd.className = 'saved-td saved-td-action'; const removeButton = document.createElement('button'); removeButton.textContent = '削除'; removeButton.className = 'remove-saved-item-button'; removeButton.dataset.rowId = rowId; removeButton.dataset.productName = productName; actionTd.appendChild(removeButton); row.appendChild(actionTd); tbody.appendChild(row); }); table.appendChild(tbody); productSection.appendChild(table); savedItemsListContainer.appendChild(productSection); }); savedItemsListContainer.querySelectorAll('.remove-saved-item-button').forEach(button => { button.addEventListener('click', handleRemoveSavedItem); }); } function handleRemoveSavedItem(event) { const { rowId, productName } = event.target.dataset; if (!rowId || !productName) return; let savedData = getSavedItemsMap(); if (savedData[productName] && savedData[productName].items[rowId]) { delete savedData[productName].items[rowId]; if (Object.keys(savedData[productName].items).length === 0) { delete savedData[productName]; } setSavedItemsMap(savedData); } const checkboxId = `checkbox-${rowId.replace(/[^a-zA-Z0-9-]/g, '_')}`; const checkboxInTable = document.getElementById(checkboxId); if (checkboxInTable) { checkboxInTable.checked = false; } renderSavedItemsList(); } if (showDialogButton && savedItemsDialog) { showDialogButton.addEventListener('click', () => { renderSavedItemsList(); savedItemsDialog.showModal(); }); } if (closeDialogButton && savedItemsDialog) { closeDialogButton.addEventListener('click', () => { savedItemsDialog.close(); }); } function getProductName(productName) { const productArray = Object.values(PRODUCTS_DATA); const foundProduct = productArray.find(product_data => { return product_data.productKey === productName; }); if (foundProduct) { return foundProduct.name; } return productName; }